{
  "name": "dynamic-route-poc",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve-json": "json-server --watch db.json --port 4000",
    "cy:open": "cypress open --config baseUrl=http://localhost:3000",
		"cy:run": "rm -rf cypress/snapshots/**/__diff_output__/ && cypress run --config baseUrl=http://localhost:3000",
		"cyupdate:run": "rm -rf cypress/snapshots/**/__diff_output__/ && cypress run --env updateSnapshots=true --config baseUrl=http://localhost:3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "express": "^4.18.2",
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "cypress": "^4.12.1",
    "cypress-image-snapshot": "^4.0.1"
  }
}
